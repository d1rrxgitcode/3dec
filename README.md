# ☕ Coffee Shop Backend API

Бэкенд для интернет-магазина кофейни, построенный на FastAPI + SQLAlchemy + PostgreSQL.

## 🚀 Технологии

- **FastAPI** - современный, быстрый веб-фреймворк для создания API
- **SQLAlchemy** - мощный ORM для работы с базой данных
- **PostgreSQL** - надежная реляционная база данных
- **Pydantic** - валидация данных и настройки
- **JWT** - аутентификация через токены
- **Alembic** - миграции базы данных

## 📋 Возможности

### Аутентификация и пользователи
- Регистрация новых пользователей
- Вход с JWT токеном
- Управление профилем пользователя
- Роли (пользователь/администратор)

### Товары (Продукты)
- CRUD операции для товаров
- Категории товаров
- Поиск по названию
- Фильтрация по категории и доступности
- Управление остатками на складе

### Заказы
- Создание заказов
- Просмотр истории заказов
- Отслеживание статуса заказа
- Отмена заказов
- Автоматический расчет стоимости
- Управление остатками при оформлении заказа

## 📁 Структура проекта

```
coffee-shop-backend/
├── app/
│   ├── __init__.py
│   ├── config.py              # Конфигурация приложения
│   ├── database.py            # Настройка подключения к БД
│   ├── dependencies.py        # Зависимости FastAPI (аутентификация)
│   ├── api/
│   │   ├── __init__.py
│   │   └── routes/
│   │       ├── __init__.py
│   │       ├── auth.py        # Регистрация и вход
│   │       ├── users.py       # Управление пользователями
│   │       ├── categories.py  # Категории товаров
│   │       ├── products.py    # Товары
│   │       └── orders.py      # Заказы
│   ├── crud/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── category.py
│   │   ├── product.py
│   │   └── order.py
│   ├── models/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── category.py
│   │   ├── product.py
│   │   └── order.py
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── category.py
│   │   ├── product.py
│   │   └── order.py
│   └── utils/
│       ├── __init__.py
│       └── security.py        # Хеширование паролей, JWT
├── alembic/                   # Миграции БД
├── main.py                    # Точка входа приложения
├── requirements.txt
├── env.example               # Пример файла окружения
├── .gitignore
└── README.md
```

## 🛠️ Установка и запуск

### Предварительные требования

- Python 3.10+
- PostgreSQL 12+
- pip

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd coffee-shop-backend
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/MacOS
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` на основе `env.example`:

```bash
cp env.example .env
```

Отредактируйте `.env` и укажите свои настройки:

```env
DATABASE_URL=postgresql://your_user:your_password@localhost:5432/coffee_shop
SECRET_KEY=your-secret-key-here
```

**Генерация SECRET_KEY:**
```bash
openssl rand -hex 32
```

### 5. Создание базы данных

```sql
-- Подключитесь к PostgreSQL и выполните:
CREATE DATABASE coffee_shop;
```

### 6. Инициализация Alembic (опционально)

Если вы хотите использовать миграции:

```bash
# Создание первой миграции
alembic revision --autogenerate -m "Initial migration"

# Применение миграций
alembic upgrade head
```

**Примечание:** При первом запуске таблицы создадутся автоматически через SQLAlchemy. Alembic нужен для последующих изменений схемы БД.

### 7. Запуск сервера

```bash
# Режим разработки (с автоперезагрузкой)
python main.py

# Или через uvicorn напрямую
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Сервер будет доступен по адресу: `http://localhost:8000`

## 📚 API Документация

После запуска сервера документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 🔐 API Эндпоинты

### Аутентификация

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| POST | `/api/v1/auth/register` | Регистрация | Все |
| POST | `/api/v1/auth/login` | Вход (получение токена) | Все |

### Пользователи

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| GET | `/api/v1/users/me` | Получить текущего пользователя | Авторизован |
| PUT | `/api/v1/users/me` | Обновить профиль | Авторизован |
| GET | `/api/v1/users/` | Список всех пользователей | Админ |
| GET | `/api/v1/users/{id}` | Получить пользователя | Админ |
| DELETE | `/api/v1/users/{id}` | Удалить пользователя | Админ |

### Категории

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| GET | `/api/v1/categories/` | Список категорий | Все |
| GET | `/api/v1/categories/{id}` | Получить категорию | Все |
| POST | `/api/v1/categories/` | Создать категорию | Админ |
| PUT | `/api/v1/categories/{id}` | Обновить категорию | Админ |
| DELETE | `/api/v1/categories/{id}` | Удалить категорию | Админ |

### Товары (Продукты)

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| GET | `/api/v1/products/` | Список товаров (с фильтрами) | Все |
| GET | `/api/v1/products/{id}` | Получить товар | Все |
| POST | `/api/v1/products/` | Создать товар | Админ |
| PUT | `/api/v1/products/{id}` | Обновить товар | Админ |
| DELETE | `/api/v1/products/{id}` | Удалить товар | Админ |

**Параметры фильтрации:**
- `category_id` - фильтр по категории
- `available_only` - только доступные товары
- `search` - поиск по названию

### Заказы

| Метод | Endpoint | Описание | Доступ |
|-------|----------|----------|--------|
| GET | `/api/v1/orders/` | Список заказов | Авторизован |
| GET | `/api/v1/orders/{id}` | Получить заказ | Владелец/Админ |
| POST | `/api/v1/orders/` | Создать заказ | Авторизован |
| PUT | `/api/v1/orders/{id}` | Обновить заказ | Владелец/Админ |
| POST | `/api/v1/orders/{id}/cancel` | Отменить заказ | Владелец/Админ |
| DELETE | `/api/v1/orders/{id}` | Удалить заказ | Админ |

## 📝 Примеры использования

### Регистрация пользователя

```bash
curl -X POST "http://localhost:8000/api/v1/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "username": "johndoe",
    "password": "securepass123",
    "full_name": "John Doe",
    "phone": "+1234567890",
    "address": "123 Main St"
  }'
```

### Вход (получение токена)

```bash
curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=user@example.com&password=securepass123"
```

Ответ:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer",
  "user": {
    "id": 1,
    "email": "user@example.com",
    "username": "johndoe",
    ...
  }
}
```

### Создание заказа

```bash
curl -X POST "http://localhost:8000/api/v1/orders/" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{
    "delivery_address": "123 Main St",
    "phone": "+1234567890",
    "notes": "Please ring the doorbell",
    "items": [
      {
        "product_id": 1,
        "quantity": 2
      },
      {
        "product_id": 3,
        "quantity": 1
      }
    ]
  }'
```

## 🗄️ Модель базы данных

### Users (Пользователи)
- id, email, username, hashed_password
- full_name, phone, address
- is_active, is_admin
- created_at, updated_at

### Categories (Категории)
- id, name, description
- image_url
- created_at, updated_at

### Products (Товары)
- id, name, description
- price, image_url
- is_available, stock_quantity
- category_id (FK)
- created_at, updated_at

### Orders (Заказы)
- id, user_id (FK)
- status (pending, confirmed, preparing, ready, delivered, cancelled)
- total_amount
- delivery_address, phone, notes
- created_at, updated_at

### OrderItems (Позиции заказа)
- id, order_id (FK), product_id (FK)
- quantity, price
- created_at

## 🔒 Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- Проверка прав доступа на уровне эндпоинтов
- Валидация всех входных данных через Pydantic

## 🧪 Тестирование

Для тестирования API используйте:
- Swagger UI (http://localhost:8000/docs)
- Postman
- curl
- httpie

## 📦 Развертывание

### Docker (рекомендуется)

Создайте `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

И `docker-compose.yml`:

```yaml
version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: coffee_shop
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://user:password@db:5432/coffee_shop
    depends_on:
      - db

volumes:
  postgres_data:
```

Запуск:
```bash
docker-compose up -d
```

## 🛣️ Roadmap

- [ ] Добавить загрузку изображений
- [ ] Реализовать систему отзывов
- [ ] Добавить систему скидок и промокодов
- [ ] Интеграция с платежными системами
- [ ] Email уведомления
- [ ] WebSocket для real-time обновлений заказов
- [ ] Кэширование (Redis)
- [ ] Unit и Integration тесты

## 🤝 Вклад в проект

Приветствуются Pull Request'ы! Для крупных изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Создано с ❤️ для обучения и демонстрации возможностей FastAPI + SQLAlchemy

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте документацию API (http://localhost:8000/docs)
2. Убедитесь, что все переменные окружения настроены правильно
3. Проверьте логи приложения
4. Откройте issue на GitHub

---

**Приятного использования! ☕**
